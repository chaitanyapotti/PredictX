{
  "name": "predict-x-webhook",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ predict-x-webhook",
    "create-local": "graph create --node http://localhost:8020/ predict-x-webhook",
    "remove-local": "graph remove --node http://localhost:8020/ predict-x-webhook",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 predict-x-webhook",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.89.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
